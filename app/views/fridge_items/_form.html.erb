<%= simple_form_for [@user, @fridge_item], url: 'fridge', remote: true do |form| %>
  <%= form.association :ingredient %>
  <%= form.input :quantity %>
  <%= form.submit "Add Item", class: "btn btn-primary" %>
<% end %>
