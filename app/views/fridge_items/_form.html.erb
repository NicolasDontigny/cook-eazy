<%= simple_form_for [@user, @fridge_item], url: 'fridge', remote: true do |form| %>
  <%= form.association :ingredient, input_html: { class: "select2", multiple: false } %>
  <%= form.input :quantity %>
  <%= form.submit "Add Item", class: "button button-main-green" %>
<% end %>
